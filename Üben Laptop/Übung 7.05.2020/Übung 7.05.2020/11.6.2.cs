using System;
using System.Collections.Generic;
using System.IO;
using System.Text;

namespace Übung_7._05._2020
{
    /*
    class _11
    {
        
        static void Main(string[] args)
        {
            int anzahl = 4;

            Console.WriteLine("Bitte geben Sie die Zahlen ein: ");

            List<int> zahlen = new List<int>();

            for (int i = 0; i < anzahl; ++i)
            {
                zahlen.Add(Convert.ToInt32(Console.ReadLine()));
            }

            string dname = "zahlen.dat";

            StreamWriter writer = new StreamWriter(dname);

            for(int i = 0; i<zahlen.Count; ++i)
            {
                writer.WriteLine(zahlen[i]);
            }

            writer.Close();
        }
    }*/
}
