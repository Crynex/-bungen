using System;
using System.Collections.Generic;
using System.Text;

namespace Übung_23._04._2020
{
    class Class8
    {
        /*
        static int Min(int x, int y)
        {
            return x < y ? x : y;
        }
        static int Max(int x, int y)
        {
            return x < y ? y : x;
        }
        static double Min(double x, double y)
        {
            return x < y ? x : y;
        }
        static double Max(double x, double y)
        {
            return x < y ? y : x;
        }
        
        static string Min(string x, string y)
        {
            if (string.Compare(x, y) < 0)
            {
                return x;
            }
            else
            {
                return y;
            }
        }

        static string Max(string x, string y)
        {
            return string.Compare(x,y) < 0 ? y: x;
        }

        static void Main()
        {
            Console.WriteLine(Min(2, 3));
            Console.WriteLine(Max(4, 5));
            Console.WriteLine(Min(2.34, 2.36));
            Console.WriteLine(Max(2.3435, 2.4534534));

            Console.WriteLine(Min("saljda", "kaskdhaks"));
            Console.WriteLine(Max("lasdal", "alsdhald"));
        }*/
    }
}
