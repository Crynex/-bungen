using System;
using System.Collections.Generic;
using System.Text;

namespace Übung_23._04._2020
{
    class Class11
    {
        /*
        static List<string> AnfangGesucht(List<string> namen, string vorname)
        {
            List<string> ergebnis = new List<string>();

            foreach(string n in namen)
            {
                if( n.IndexOf(vorname) == 0 && (n.Length == vorname.Length || n[vorname.Length] == ' ')) ergebnis.Add(n);
            }

            return ergebnis;
        }

        static void Main()
        {
            List<string> feld = new List<string>()
            {
                "Hans Meier",
                "Ulla Becker",
                "Beate Schmidt",
                "Hans Müller",
                "Michael Maier",
                "Hans Thomas",
                "Hans-Peter Maier",
            };

            List<string> ergebnis = AnfangGesucht(feld, "Hans");
            foreach (string n in ergebnis) Console.WriteLine(n);
        }*/
            
    }
}
