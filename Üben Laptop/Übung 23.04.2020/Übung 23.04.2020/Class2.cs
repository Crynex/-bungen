using System;
using System.Collections.Generic;
using System.Text;

namespace Übung_23._04._2020
{
    class Class2

    {
        /*
        static int Abs(int zahl)
        {
            return zahl < 0 ? -zahl : zahl;
        }

        static int Min(int x, int y)
        {
            if (x < y)
            {
                return x;
            }
            else
            {
                return y;
            }
        }

        static int Max(int x, int y)
        {
            if(x > y)
            {
                return x;
            }
            else
            {
                return y;
            }
        }

        static bool Gerade(int zahl)
        {
            return (zahl % 2 == 1);
        }

        static void Main()
        {
            Console.WriteLine(Abs(3));
            Console.WriteLine(Abs(0));
            Console.WriteLine(Abs(-5));
            Console.WriteLine(Min(8, 5));
            Console.WriteLine(Min(5, 5));
            Console.WriteLine(Max(8, 5));
            Console.WriteLine(Max(5, 32));
            Console.WriteLine(Gerade(8));
            Console.WriteLine(Gerade(5));
        }*/
    }
}
