using System;
using System.Collections.Generic;
using System.Text;

namespace Übung_23._04._2020
{
    class Class12
    {
        /*
        static List<int> AlleMaxima(List<double> zahlen)
        {
            List<int> ergebnis = new List<int>();

            double max = double.MinValue;
            for(int i = 0; i < zahlen.Count; ++i)
            {
                if(zahlen[i] > max)
                {
                    ergebnis.Clear();
                    max = zahlen[i];
                    ergebnis.Add(i);
                }
                else if(zahlen[i] == max)
                {
                    ergebnis.Add(i);
                }
            }

            return ergebnis;
        }

        static void Main()
        {
            List<double> zahlen = new List<double>() { 1, 2, 3, 3, 4, 4, 2, 3, 4, 3, 2, 3, 4, };

            List<int> position = AlleMaxima(zahlen);
            foreach(int k in position)
            {
                Console.WriteLine(k + " - " + zahlen[k]);
            }
        }*/
    }
}
