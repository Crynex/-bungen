using System;
using System.Collections.Generic;
using System.Text;

namespace Übung_23._04._2020
{
    class Class10
    {
        /*
        static double DSchnitt(List<double> zahlen)
        {
            if (zahlen.Count == 0) return 0.0;

            double summe = 0;
            for(int i = 0; i < zahlen.Count; ++i)
            {
                summe += zahlen[i];
            }

            return summe / zahlen.Count;
        }

        static void MinMax(List<double> zahlen, int anz, ref double max, ref double min)
        {
            max = double.MinValue;
            min = double.MaxValue;
            for(int i = 0; i < anz;  ++i)
            {
                if( max < zahlen[i])
                {
                    max = zahlen[i];
                }
                if( min > zahlen[i])
                {
                    min = zahlen[i];
                }
            }
        }


        static void Main()
        {
            List<double> zahlen = new List<double>() { 1.4232, 2332.23, 232.232, 1.21, 1.23123, 1.23132, 1.45345, 52424.232 };
            Console.WriteLine(DSchnitt(zahlen));

            double min = 0;
            double max = 0;
            MinMax(zahlen, zahlen.Count, ref max, ref min);
            Console.WriteLine(min);
            Console.WriteLine(max);
            MinMax(zahlen, 4, ref max, ref min);

            Console.WriteLine(min);
            Console.WriteLine(max);

        }*/
    }
}
