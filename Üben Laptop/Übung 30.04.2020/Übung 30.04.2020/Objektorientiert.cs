namespace Übung_30._04._2020
{
    /*
    public struct Auto
    {
        public double kmstand;
        public double tank;
        public string besitzer;

        public Auto(double KmStand, double TankInhalt, string Besitzer)
        {           
        this.kmstand = KmStand;
		this.tank = TankInhalt;
        this.besitzer = Besitzer;
        }

        //Ausgeben
        public void Ausgeben()
        {
            Console.WriteLine("Der Besitzer des Autos ist: " + this.besitzer);
            Console.WriteLine("Der KMStand ist " + this.kmstand + " km");
            Console.WriteLine("Der Tankinhalt ist " + this.tank + " Liter");
        }

        public void Fahren(double distance)
        {
            this.kmstand = this.kmstand + distance;
            this.tank = this.tank - distance*0.05;
        }
    }

    class Program
    {
        static void Main(string[] args)
        {
        Auto meins = new Auto(0, 30, "Ich");

            meins.Ausgeben();

            meins.Fahren(100);

            meins.Ausgeben();
        }
    }*/
}
