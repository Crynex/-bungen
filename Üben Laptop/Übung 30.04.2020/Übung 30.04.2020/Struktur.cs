namespace Übung_30._04._2020
{
    class Struktur
    {
        /*
        public struct Angestellter
        {
            public string Name;
            public int ID;                       
        }

        class StructBsp
        {
            static void Main()
            {
                List<Angestellter> abteilung = new List<Angestellter>();

                for (int i = 0; i < 5; ++i)
                {
                    Angestellter neuer;
                    Console.WriteLine("Bitte geben Sie einen Namen ein: ");
                    neuer.Name = Console.ReadLine();

                    Console.WriteLine("Bitte geben Sie die ID ein: ");
                    neuer.ID = Convert.ToInt32(Console.ReadLine());

                    abteilung.Add(neuer);
                }
                
                for (int i = 0; i < abteilung.Count; ++i)
                {
                    Console.WriteLine(abteilung[i].Name + ";" + abteilung[i].ID);
                }

                if (Suche(abteilung, "Hans"))
                {
                    Console.WriteLine("Hans arbeitet hier");
                }
            }

            static bool Suche(List<Angestellter> abteilung, string gesuchterName)
            {
                for (int zl = 0; zl < abteilung.Count; ++zl)
                {
                    if (abteilung[zl].Name == gesuchterName)
                        return true;
                }
                return false;
            }
        
        }*/
    }
}
