namespace Übung_30._04._2020
{
    class Funktionsorientiert
    {
        /*
        //Auto
        public struct Auto
        {
            public double kmstand;
            public double tank;
            public string besitzer;

            public Auto(double KmStand, double TankInhalt, string Besitzer)
            {
                this.kmstand = KmStand;
                this.tank = TankInhalt;
                this.besitzer = Besitzer;
            }

            //Ausgeben
            public static void Ausgeben(Auto dasAuto)
            {
                Console.WriteLine("Der Besitzer des Autos ist: " + dasAuto.besitzer);
                Console.WriteLine("Der KMStand ist " + dasAuto.kmstand + " km");
                Console.WriteLine("Der Tankinhalt ist " + dasAuto.tank + " Liter");
            }

            //Fahren
            public static void Fahren(ref Auto dasAuto, double distance)
            {
                dasAuto.kmstand = dasAuto.kmstand + distance;
                dasAuto.tank = dasAuto.tank - distance * 0.05;
            }
        }

        class Program
        {
            static void Main(string[] args)
            {
                Auto meins = new Auto(0, 30, "Ich");

                Auto.Ausgeben(meins);

                Auto.Fahren(ref meins, 100);

                Auto.Ausgeben(meins);
            }
        }*/
    }
}
