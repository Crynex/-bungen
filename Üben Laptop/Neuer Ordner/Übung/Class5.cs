using System;
using System.Collections.Generic;
using System.Text;

namespace Übung
{
    class Class5
    {
        /*
        static void Main()
        {
            Console.WriteLine("Geben Sie ganze Zahlen ein: ");
            List<int> zahlen = new List<int>();
            string eingabe = Console.ReadLine();
            while(eingabe.ToUpper() != "ENDE")
            {
                zahlen.Add(Convert.ToInt32(eingabe));
                eingabe = Console.ReadLine();
            }

            int gerade = 0;
            int ungerade = 0;

            foreach(int zahl in zahlen)
            {
                if(zahl % 2 == 0)
                {
                    gerade += zahl;
                }
                else
                {
                    ungerade += zahl;
                }
            }

            Console.WriteLine(gerade);
            Console.WriteLine(ungerade);
        } */
    }
}
