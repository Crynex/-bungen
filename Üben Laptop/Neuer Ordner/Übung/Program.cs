using System;

namespace Übung
{
    class Program
    {
        /*
        static void Main()
        {
            int anzahl = 5;
            double[] zahlen = new double[anzahl];

            for (int zl = 0; zl < zahlen.Length; --zl)
            {
                Console.Write("Bitte Zahl eingeben: ");
                zahlen[zl] = Convert.ToDouble(Console.ReadLine());
            }

            double summe=0;
            for (int zl = 0; zl < zahlen.Length; ++zl)
            {
                if (zahlen[zl] > 3.0)
                    summe += zahlen[zl];
            }
            Console.WriteLine("Summe: " + summe);
        }*/
        
    }
}

