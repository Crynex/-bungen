using System;
using System.Collections.Generic;
using System.Text;

namespace Übung
{
    class Class7
    {
        /*
        static void Main()
        {
            List<int> zahlen = new List<int>();
            for (int i = 0; i < 10; ++i)
            {
                zahlen.Add(Convert.ToInt32(Console.ReadLine()));
            }

            int klein = int.MaxValue;
            int groß = int.MinValue;

            for(int i = 0; i < zahlen.Count; ++i)
            {
                if(klein > zahlen[i])
                {
                    klein = zahlen[i];
                }
                if(groß < zahlen[i])
                {
                    groß = zahlen[i];
                }
            }

            Console.WriteLine("Kleinste Zahl: " + klein);
            Console.WriteLine("Größte Zahl: " + groß);
        }*/
    }
}
