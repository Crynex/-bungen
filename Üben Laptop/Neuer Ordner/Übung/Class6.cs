using System;
using System.Collections.Generic;
using System.Text;

namespace Übung
{
    class Class6
    {
        /*
        static void Main()
        {
            Console.WriteLine("Geben Sie eine Anzahl ein: ");
            int anz = Convert.ToInt32(Console.ReadLine());

            List<int> zahlen = new List<int>();
            for(int i = 0; i < anz; ++i)
            {
                zahlen.Add(Convert.ToInt32(Console.ReadLine()));
            }

            for(int k = 0; k < anz; ++k)
            {
                Console.Write(zahlen[k] + " ");
            }

            int negativ = 0;
            for(int i = 0; i < anz; ++i)
            {
                if(zahlen[i] < 0)
                {
                    ++negativ;
                }
            }

            Console.WriteLine("Es wurden " + negativ + "negative Zahlen eingegeben");
        }*/
    }
}
