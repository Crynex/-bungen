using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Text;

namespace Übung
{
    class Class1
    {
        /*
        static void Main()
        {
            double[] zahlen = new double[5];

            for(int i = 0; i < zahlen.Length; ++i)
            {
                Console.WriteLine("Bitte geben Sie eine Zahl ein: ");
                string eingabe = Console.ReadLine();
                zahlen[i] = Convert.ToDouble(eingabe);
            }

            Console.WriteLine("Die Zahlen sind: ");
            for(int i = 0; i < zahlen.Length; ++i)
            {
                Console.WriteLine(zahlen[i]); 
            }

            double dschnitt = 0;
            for (int i = 0; i < zahlen.Length; ++i)
            {
                dschnitt += zahlen[i];
            }
            Console.WriteLine("Der Durschnitt ist: " + dschnitt / zahlen.Length);

            double max = double.MinValue;
            for(int i = 0; i < zahlen.Length; ++i)
            {
                if(zahlen[i] > max)
                {
                    max = zahlen[i];
                }
            }
            Console.WriteLine("Das Maximum ist: " + max);
        }*/
    }
}
