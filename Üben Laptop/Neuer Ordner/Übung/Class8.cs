using System;
using System.Collections.Generic;
using System.Text;

namespace Übung
{
    class Class8
    {
        /*
        static void Main()
        {
            int anz = Convert.ToInt32(Console.ReadLine());

            List<string> namen = new List<string>();
            for(int i = 0; i < anz; ++i)
            {
                namen.Add(Console.ReadLine());
            }

            for(int i = 0; i < namen.Count; ++i)
            {
                Console.WriteLine(namen[i]);
            }

            int anamen = 0;
            for(int i = 0; i < namen.Count; ++i)
            {
                if(namen[i].Length > 0 && namen[i][0] == 'A')
                {
                    Console.WriteLine(namen[i]);
                    ++anamen;
                }
            }

            Console.WriteLine(anamen);
        }*/
    }

}
