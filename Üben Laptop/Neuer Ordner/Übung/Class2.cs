using System;
using System.Collections.Generic;
using System.Text;

namespace Übung
{
    class Class2
    {
        /*
        static void Main()
        {
            Console.Write("Geben Sie eine Anzahl ein: ");
            double anz = Convert.ToDouble(Console.ReadLine());

            while(anz == 0)
            {
                Console.WriteLine("Bitte geben Sie eine Zahl > 0 ein: ");
                anz = Convert.ToDouble(Console.ReadLine());
            }

            List<double> zahlen = new List<double>();

            for(int i = 0; i < anz; ++i)
            {
                zahlen.Add(Convert.ToDouble(Console.ReadLine()));
            }

            double dschnitt = 0;
            for(int i = 0; i < zahlen.Count; ++i)
            {
                dschnitt += zahlen[i];
            }

            Console.WriteLine("Der Dschnitt ist: " + (double)dschnitt / zahlen.Count);
        }*/
    }
}
