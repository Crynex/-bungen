using System;
using System.Collections.Generic;
using System.Text;

namespace Üben
{
    /*
    class Übung_5
    {
        static void Main()
        {
            Console.WriteLine("Bitte geben Sie die Anzahl ein: ");
            int anz = 0;

            while (anz == 0)
            {
                anz = Convert.ToInt32(Console.ReadLine());
                if (anz == 0)
                {
                    Console.Write("Bitte geben Sie eine Zahl größer 0 ein.");
                }
            }

            List<string> eingabe = new List<string>(); 

            for(int i = 0; i < anz; ++i)
            {
                eingabe.Add(Convert.ToString(Console.ReadLine()));

            }

            for(int i = anz-1; i>=0; --i)
            {
                Console.Write(eingabe[i]);
            }
        }
    }*/
}
